# PostgreSQL — mobil + backend entegrasyonu için local DB
POSTGRES_USER=postgres
POSTGRES_PASSWORD=dev
POSTGRES_DB=cardence
POSTGRES_PORT=5432

# Backend connection string (Cardence.Api appsettings ile aynı)
# Host=localhost;Port=5432;Database=cardence;Username=postgres;Password=dev
