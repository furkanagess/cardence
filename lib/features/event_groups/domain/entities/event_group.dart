/// Etkinlik grubu (konferans, networking vb.); kartlar bu gruplara bağlanabilir.
class EventGroup {
  const EventGroup({
    required this.id,
    required this.name,
  });

  final String id;
  final String name;
}
